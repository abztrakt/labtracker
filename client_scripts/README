= INSTALLATION =

 Depends on:
  - py2exe
  - python 2.6
 
 Required modules:
  - pywin32

== Windows ==
 Run
    c:\python26\python.exe setup.py py2exe

 This will compile the python files into Windows executables (.exe)

 The contents of the 'dist' folder will contain all necessary files.

 Copy the dist folder onto client machines. Using group policy, accessed by typing 'mmc' (without quotes) into the Windows run command, add the tracker.exe to the logon and logoff scripts.  For parameters, pass in '-a login' for the logon script and '-a logout' for the logoff script.

== OS X ==
 Run 
    sudo ./install_or_update_on_mac.sh

= UNINSTALL =

== OS X ==
 Run
    sudo ./uninstall_on_mac.sh

= TROUBLESHOOTING =
    TODO
